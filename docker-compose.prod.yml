# Production override for Raspberry Pi with pre-built images
# Usage: docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d
services:
  printer_service:
    image: ghcr.io/nilp0inter/frostbyte-printer:latest
    build: !reset null
    privileged: true
    environment:
      DRY_RUN: "false"
      BROTHER_QL_PRINTER: "usb://0x04f9:0x209b/000H2G261629"
      BROTHER_QL_MODEL: "QL-800"
    volumes:
      - /dev/bus/usb:/dev/bus/usb

  client_builder:
    image: ghcr.io/nilp0inter/frostbyte-client:latest
    command: ["sh", "-c", "cp -r /dist/* /output/"]
    volumes:
      - client_dist:/output
